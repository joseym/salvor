//! Salvor CLI: `run`, `resume`, `list`, `history`, and `replay --dry-run`
//! over durable agent runs.

fn main() {
    println!("salvor: durable execution runtime for AI agents (v0.1 scaffold)");
}
